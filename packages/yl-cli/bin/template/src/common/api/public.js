/**
 * 下拉接口列表
//  */
// import * as URL from './public.url'
// import fetch from '@public/http/request'

/** *********************************************** 接口请求 ******************************************* */
/**
 * demo
 * @param { Object } params
 * @returns Promise<T> T: { records?: [ { ... } ] }
 * @status
 * @description
 */
// export const reqCheckCoupon = (params = {}) => {
//   // params.size = params.size || 5000
//   return fetch.get(`${URL.checkCoupon}`, { params })
// }
