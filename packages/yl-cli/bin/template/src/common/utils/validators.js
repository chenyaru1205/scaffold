// const { lang } = $ylI18n
const { RULES: rules, VALIDATION: validation } = $ylConfig
// 初始化规则
export const RULES = {
  ...rules,
  ...{
  }
}
export const VALIDATION = {
  ...validation,
  ...{
  }
}
